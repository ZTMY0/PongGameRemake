{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe", // Points to the executable generated by the build task
            "args": [],
            "cwd": "${fileDirname}", // Set the working directory to the directory of the active file
            "preLaunchTask": "C/C++: g++.exe build active file (MSYS2)" // Ensures the build task runs before debugging
        }
    ]
}